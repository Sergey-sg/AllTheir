<div class="container-fluid footer bg-black" style="margin-top: 50px">
  <footer class="py-3">
    <div class="row">
      <div class="col-2">
        <h5>{{ _('Partners') }} </h5>
      </div>
      <div class="col-9">
          {% for item in menu_items %}
             {% if item.position == 'f' %}
                <a class="text-muted" href="{{ item.item_url }}" target="{{ item.target }}">{{ item.title }}</a>
             {% endif %}
          {% endfor %}
      </div>
    </div>

    <div class="border-top"></div>
    <br>
      <p class="text-center">© 2022 <a href="#">{{ _('Sergey Glushak')}}</a>, {{ _('All rights reserved.') }}</p>
  </footer>
</div>